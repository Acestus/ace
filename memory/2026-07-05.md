# Daily Note — Sunday, 2026-07-05

## Rounds Board
All 5 lanes empty (0/5 WIP). Nothing currently claimed.

## Active Work
- **ACE-24** — ISN Interview Prep (In Progress, High priority, flow:queue)
  - Not claimed into a lane yet.

## Queue (flow:queue, Backlog) — Daily Gospel Reading series
- ACE-40 — AWS Marketplace SaaS (Medium)
- ACE-39 — Google Cloud AI Agent (Medium)
- ACE-38 — Catholic Daily Gospel GPT / GPT Store (Medium)
- ACE-37 — GitHub Copilot CLI Plugin (Medium)
- ACE-36 — OpenClaw Skill (Medium)
- ACE-35 — Claude Code Skill (Medium)
- ACE-34 — Android App MVP (Medium)
- ACE-33 — iOS App MVP (Medium)

## Notes
- `dispatch-next` suggests **ACE-24** as the top pick if starting fresh work today.
- The 8 Gospel-reading backlog tickets are all Medium priority, same theme (multi-platform Gospel reading app), still unclaimed.
- ⚠️ Heads up: the `start-my-day` skill points at a `planner start-my-day`/`daily-note` CLI command that no longer exists in `Ace.Tools.Cli` — `Program.cs` was rewritten and `WorkflowHandler.cs` (which has the real `start-my-day`/`end-my-day` logic) is orphaned/unwired. I built today's board manually via `linear search` + `rounds status`. Worth fixing the CLI wiring or updating the skill so this doesn't need a manual workaround tomorrow.
