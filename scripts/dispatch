#!/usr/bin/env bash
# dispatch — native CLI shim
set -euo pipefail
PROJECTS_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
exec dotnet run --project "$PROJECTS_DIR/src/Ace.Tools.Cli" -- linear dispatch-next "$@"
